# EDC adapter overrides for vm-distributed topology.
# Overrides the shared infrastructure VM_PROVIDER_CONNECTORS / VM_CONSUMER_CONNECTORS
# which default to the inesdata connector short-names (citycouncil, company).
VM_PROVIDER_CONNECTORS=citycounciledc
VM_CONSUMER_CONNECTORS=companyedc
